# ──────────────────────────────────────────────
# 💚 openSUSE Kudos — Netlify Deploy Preview Env
# Just a copy of what's otherwise set in netlify
# ──────────────────────────────────────────────

# --- Core runtime ---
NODE_ENV=production
NODE_VERSION=20
AUTH_MODE=LOCAL

# --- Database ---
# SQLite is fine for preview builds — seeded automatically on deploy.
# DB has to be in writeable location
PRISMA_CLIENT_ENGINE_TYPE=binary
DATABASE_URL="file:/tmp/dev.db"

# --- Session / cookies ---
SESSION_SECRET=netlify-preview-secret

# --- Networking / URLs ---
# Netlify injects DEPLOY_PRIME_URL and URL automatically,
# so BACKEND_ORIGIN is detected dynamically in app.js.
CORS_ALLOWED_ORIGINS=https://*.netlify.app

# --- Frontend build config (Vite) ---
VITE_BACKEND_URL=/api
VITE_APP_ENV=preview
VITE_APP_NAME=openSUSE Kudos

# --- Optional debug flags ---
# DEBUG=express:*,app:*

